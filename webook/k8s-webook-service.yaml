apiVersion: v1
kind: Service
metadata:
  name: webook-live
spec:
  type: LoadBalancer
  selector:
    app: webook-live
  ports:
# Cluster IP
    - protocol: TCP
      name: http
      port: 81
      targetPort: 8080

  